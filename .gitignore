# Private docs — not for public distribution
private/

# Studio build outputs (produced by `go build` in the studio/ module).
studio/rpasm
studio/rpasm-studio
studio/dist/

# JetBrains IDE configs (local, per-user)
.idea/

# JetBrains IDE configs (local, per-user).
.idea/

# JetBrains IDE configs (local, per-user).
.idea/
